<Window x:Class="Projektsoftware.Views.TicketEmailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="E-Mail an Kunden senden"
        Height="560" Width="580"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📧 E-Mail senden"
                       FontSize="22" FontWeight="Bold"
                       Foreground="#2196F3"/>
            <Border Background="#E3F2FD" CornerRadius="4"
                    Padding="10,6" Margin="0,8,0,0">
                <TextBlock x:Name="TicketInfoTextBlock"
                           FontSize="13" Foreground="#1565C0"
                           TextWrapping="Wrap"/>
            </Border>
        </StackPanel>

        <!-- An -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="An:" VerticalAlignment="Center"
                       FontWeight="SemiBold" FontSize="13"/>
            <TextBox x:Name="ToTextBox" Grid.Column="1"
                     Height="32" FontSize="13"
                     VerticalContentAlignment="Center" Padding="6,0"/>
        </Grid>

        <!-- CC -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="CC:" VerticalAlignment="Center"
                       FontSize="13" Foreground="#666"/>
            <TextBox x:Name="CcTextBox" Grid.Column="1"
                     Height="32" FontSize="13"
                     VerticalContentAlignment="Center" Padding="6,0"/>
        </Grid>

        <!-- Betreff -->
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Betreff:" VerticalAlignment="Center"
                       FontWeight="SemiBold" FontSize="13"/>
            <TextBox x:Name="SubjectTextBox" Grid.Column="1"
                     Height="32" FontSize="13"
                     VerticalContentAlignment="Center" Padding="6,0"/>
        </Grid>

        <!-- Nachricht Label -->
        <TextBlock Grid.Row="4" Text="Nachricht:"
                   FontWeight="SemiBold" FontSize="13"
                   Margin="0,0,0,4"/>

        <!-- Nachricht -->
        <TextBox x:Name="MessageTextBox" Grid.Row="5"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="13" Padding="8"
                 VerticalContentAlignment="Top"/>

        <!-- Buttons -->
        <Grid Grid.Row="6" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="⚙️ Ticket-SMTP unter Einstellungen → Ticket-SMTP konfigurieren"
                       FontSize="11" Foreground="#999"
                       VerticalAlignment="Center"/>
            <Button x:Name="SendButton" Grid.Column="1"
                    Content="📧 Senden"
                    Width="120" Height="36"
                    Click="Send_Click"
                    Background="#4CAF50" Foreground="White"
                    FontWeight="SemiBold" FontSize="13"/>
            <Button Grid.Column="3"
                    Content="Abbrechen"
                    Width="100" Height="36"
                    Click="Cancel_Click"/>
        </Grid>
    </Grid>
</Window>
