<Window x:Class="Projektsoftware.Views.PortalOrdersDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🛒 Webshop-Bestellungen"
        Height="640" Width="820"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="🛒 Webshop-Bestellungen"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="#2196F3"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
            <ComboBox x:Name="StatusFilter" Width="200" SelectedIndex="0" SelectionChanged="StatusFilter_Changed">
                <ComboBoxItem Content="🆕 Nur neue Bestellungen"/>
                <ComboBoxItem Content="Alle Bestellungen"/>
                <ComboBoxItem Content="⏳ In Bearbeitung"/>
                <ComboBoxItem Content="✅ Erledigt"/>
            </ComboBox>
            <Button Content="🔄 Aktualisieren"
                    Margin="12,0,0,0" Padding="12,4"
                    Click="Refresh_Click"/>
        </StackPanel>

        <Border Grid.Row="2"
                BorderBrush="#DDDDDD"
                BorderThickness="1"
                CornerRadius="6">
            <ListBox x:Name="OrdersList"
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Focusable" Value="False"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#EEEEEE"
                                BorderThickness="0,0,0,1"
                                Padding="14,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding OrderNumber}"
                                                   FontWeight="Bold" FontSize="15" Foreground="#222"/>
                                        <TextBlock Text="{Binding StatusText}"
                                                   FontSize="12" Foreground="#666"
                                                   Margin="12,2,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding CustomerName}"
                                               FontSize="13" Foreground="#444" Margin="0,3,0,0"/>
                                    <TextBlock Text="{Binding ItemSummary}"
                                               FontSize="12" Foreground="#777"
                                               TextWrapping="Wrap" Margin="0,3,0,0"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="{Binding CreatedAtText}" FontSize="11" Foreground="#999"/>
                                        <TextBlock Text=" · " FontSize="11" Foreground="#999"/>
                                        <TextBlock Text="{Binding PaymentMethodText}" FontSize="11" Foreground="#999"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding TotalGross, StringFormat='{}{0:N2} €'}"
                                               FontWeight="Bold" FontSize="16" Foreground="#222"
                                               HorizontalAlignment="Right"/>
                                    <TextBlock Text="brutto" FontSize="11" Foreground="#999" HorizontalAlignment="Right"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <Button Content="In Bearbeitung"
                                                Tag="{Binding Id}"
                                                Padding="8,4" Margin="0,0,6,0"
                                                Background="#FFF3E0" BorderBrush="#FFB74D"
                                                Click="SetInProgress_Click"/>
                                        <Button Content="Erledigt"
                                                Tag="{Binding Id}"
                                                Padding="8,4" Margin="0,0,6,0"
                                                Background="#E8F5E9" BorderBrush="#81C784"
                                                Click="SetDone_Click"/>
                                        <Button Content="Stornieren"
                                                Tag="{Binding Id}"
                                                Padding="8,4"
                                                Background="#FFEBEE" BorderBrush="#E57373"
                                                Click="SetCancelled_Click"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <TextBlock x:Name="EmptyText"
                   Grid.Row="2"
                   Text="📭 Keine Bestellungen vorhanden"
                   FontSize="16"
                   Foreground="#999"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>

        <Button Grid.Row="3"
                Content="Schließen"
                Width="100"
                Height="35"
                HorizontalAlignment="Right"
                Margin="0,15,0,0"
                Click="Close_Click"/>
    </Grid>
</Window>
