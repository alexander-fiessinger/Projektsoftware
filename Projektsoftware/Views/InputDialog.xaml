<Window x:Class="Projektsoftware.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Eingabe" Height="320" Width="500"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="CanResize"
        Background="#1E1E1E"
        Topmost="True"
        ShowInTaskbar="False"
        MinHeight="300" MinWidth="450">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" 
                   Grid.Row="0" 
                   Text="Eingabe erforderlich"
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="White"
                   Margin="0,0,0,15"/>

        <TextBlock x:Name="MessageText" 
                   Grid.Row="1" 
                   Text="Bitte geben Sie einen Wert ein:"
                   TextWrapping="Wrap"
                   Foreground="#CCCCCC"
                   Margin="0,0,0,15"/>

        <TextBox x:Name="InputTextBox" 
                 Grid.Row="2" 
                 Height="35"
                 FontSize="14"
                 Padding="8"
                 Background="#2D2D30"
                 Foreground="White"
                 BorderBrush="#555555"
                 BorderThickness="1"
                 KeyDown="InputTextBox_KeyDown"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="OK" 
                    Width="100" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#4EC9B0" 
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="Bold"
                    Cursor="Hand"
                    Click="OK_Click"/>
            <Button Content="Abbrechen" 
                    Width="100" 
                    Height="35"
                    Background="#555555" 
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
