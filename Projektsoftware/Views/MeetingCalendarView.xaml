<UserControl x:Class="Projektsoftware.Views.MeetingCalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="CalNavButton" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
        </Style>

        <Style x:Key="DayHeaderText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="MeetingChip" TargetType="Border">
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="1,1,1,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="DayButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="290"/>
        </Grid.ColumnDefinitions>

        <!-- ═══ LEFT: Calendar ═══ -->
        <Border Grid.Column="0" Background="White" CornerRadius="10"
                BorderBrush="#E0E0E0" BorderThickness="1">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header: Navigation + Neues Meeting -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Navigation -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="‹" Style="{StaticResource CalNavButton}"
                                Click="PrevMonth_Click" ToolTip="Vorheriger Monat"/>
                        <TextBlock x:Name="MonthYearTextBlock"
                                   FontSize="18" FontWeight="Bold"
                                   VerticalAlignment="Center" Margin="8,0"
                                   MinWidth="200"
                                   Foreground="{StaticResource PrimaryBrush}"/>
                        <Button Content="›" Style="{StaticResource CalNavButton}"
                                Click="NextMonth_Click" ToolTip="Nächster Monat"/>
                        <Button Content="Heute" Margin="8,0,0,0" Height="28" Padding="10,0"
                                Background="#E3F2FD" Foreground="{StaticResource PrimaryBrush}"
                                BorderThickness="0" Cursor="Hand" FontSize="12"
                                Click="Today_Click"/>
                    </StackPanel>

                    <!-- New Meeting button -->
                    <Button Grid.Column="2"
                            Content="+ Neues Meeting"
                            Height="32" Padding="12,0"
                            Click="AddMeeting_Click"
                            Background="#4CAF50" Foreground="White"
                            BorderThickness="0" Cursor="Hand" FontWeight="SemiBold"/>
                </Grid>

                <!-- Day-of-week headers -->
                <Grid Grid.Row="1" x:Name="WeekdayHeaderGrid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Mo" Style="{StaticResource DayHeaderText}"/>
                    <TextBlock Grid.Column="1" Text="Di" Style="{StaticResource DayHeaderText}"/>
                    <TextBlock Grid.Column="2" Text="Mi" Style="{StaticResource DayHeaderText}"/>
                    <TextBlock Grid.Column="3" Text="Do" Style="{StaticResource DayHeaderText}"/>
                    <TextBlock Grid.Column="4" Text="Fr" Style="{StaticResource DayHeaderText}"/>
                    <TextBlock Grid.Column="5" Text="Sa" Style="{StaticResource DayHeaderText}" Foreground="#F44336"/>
                    <TextBlock Grid.Column="6" Text="So" Style="{StaticResource DayHeaderText}" Foreground="#F44336"/>
                </Grid>

                <Separator Grid.Row="2" Margin="0,0,0,8"/>

                <!-- Calendar Grid (6 rows × 7 cols) -->
                <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Disabled">
                    <Grid x:Name="CalendarGrid"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- ═══ RIGHT: Day Detail Panel ═══ -->
        <Border Grid.Column="2" Background="White" CornerRadius="10"
                BorderBrush="#E0E0E0" BorderThickness="1">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Day header -->
                <TextBlock x:Name="DayDetailHeaderTextBlock"
                           Grid.Row="0"
                           FontSize="16" FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrush}"
                           Margin="0,0,0,10"
                           TextWrapping="Wrap"/>

                <Separator Grid.Row="1" Margin="0,0,0,10"/>

                <!-- Meeting list -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="DayMeetingsPanel"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
