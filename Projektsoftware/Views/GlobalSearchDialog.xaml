<Window x:Class="Projektsoftware.Views.GlobalSearchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Globale Suche" Height="520" Width="780"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Suche über Projekte, Tickets, Aufgaben und Mitarbeiter"
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <Button x:Name="SearchButton" DockPanel.Dock="Right" Content="Suchen"
                    Width="90" Margin="8,0,0,0" Click="SearchButton_Click" IsDefault="True"/>
            <TextBox x:Name="QueryBox" Padding="6" FontSize="13"
                     KeyDown="QueryBox_KeyDown"/>
        </DockPanel>

        <DataGrid Grid.Row="2" x:Name="ResultsGrid" AutoGenerateColumns="False"
                  IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Typ" Binding="{Binding Type}" Width="120"/>
                <DataGridTextColumn Header="Titel" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Relevanz" Binding="{Binding Relevance}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="3" Margin="0,8,0,0">
            <TextBlock x:Name="StatusText" Text="Bereit"/>
        </StatusBar>
    </Grid>
</Window>
