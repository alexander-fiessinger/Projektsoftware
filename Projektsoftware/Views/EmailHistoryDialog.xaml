<Window x:Class="Projektsoftware.Views.EmailHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="E-Mail-Verlauf pro Kontakt" Height="560" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="E-Mail-Verlauf" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Kunde:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Button DockPanel.Dock="Right" Content="Anzeigen" Width="100" Margin="8,0,0,0" Click="Show_Click"/>
            <ComboBox x:Name="ContactCombo" DisplayMemberPath="DisplayName"/>
        </DockPanel>

        <DataGrid Grid.Row="2" x:Name="EmailGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="" Binding="{Binding DirectionIcon}" Width="40"/>
                <DataGridTextColumn Header="Datum" Binding="{Binding FormattedDate}" Width="140"/>
                <DataGridTextColumn Header="Von" Binding="{Binding FromAddress}" Width="180"/>
                <DataGridTextColumn Header="An" Binding="{Binding ToAddress}" Width="180"/>
                <DataGridTextColumn Header="Betreff" Binding="{Binding Subject}" Width="*"/>
                <DataGridTextColumn Header="Vorschau" Binding="{Binding ShortPreview}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Schließen" Width="100" Click="Close_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
