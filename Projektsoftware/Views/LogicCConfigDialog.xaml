<Window x:Class="Projektsoftware.Views.LogicCConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LogicC AI Konfiguration" 
        Height="650" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="650"
        MinWidth="600"
        Background="#1E1E1E">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3E3E3E"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3E3E3E"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1177BB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#3E3E3E"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4E4E4E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3E3E3E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Grid.Column="2"
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="#2D2D2D"
                                          BorderBrush="#3E3E3E"
                                          BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="ContentSite"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      Margin="8,3,0,3"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      TextElement.Foreground="White"/>
                                    <Path x:Name="Arrow"
                                          Grid.Column="1"
                                          Fill="White"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                                </Grid>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#2D2D2D"
                                            BorderThickness="1"
                                            BorderBrush="#3E3E3E"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0E639C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0E639C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🤖 LogicC AI Integration" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="White"/>
            <TextBlock Text="Konfigurieren Sie Ihre LogicC AI API-Verbindung" 
                       FontSize="12" 
                       Foreground="#AAAAAA" 
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- API Key -->
        <StackPanel Grid.Row="1">
            <Label Content="API Key:" FontWeight="Bold"/>
            <PasswordBox x:Name="ApiKeyPasswordBox" 
                         PasswordChanged="ApiKeyPasswordBox_PasswordChanged"/>
            <TextBlock TextWrapping="Wrap"
                       FontSize="10" 
                       Foreground="#FF6B6B" 
                       Margin="0,-5,0,0">
                <Run Text="⚠️ Wichtig: Verwenden Sie einen "/>
                <Run Text="LineLLM Virtual Key" FontWeight="Bold"/>
                <Run Text=" vom LogicC Support (aktive Subscription erforderlich)"/>
            </TextBlock>
        </StackPanel>

        <!-- API URL -->
        <StackPanel Grid.Row="2">
            <Label Content="API URL:" FontWeight="Bold"/>
            <TextBox x:Name="ApiUrlTextBox" 
                     Text="https://api.logicc.io/v1"/>
        </StackPanel>

        <!-- Model Selection -->
        <StackPanel Grid.Row="3">
            <Label Content="KI-Modell:" FontWeight="Bold"/>
            <ComboBox x:Name="ModelComboBox" 
                      SelectedIndex="0"
                      Padding="8"
                      Margin="0,5,0,10">
                <ComboBoxItem Content="gpt-4o" Tag="Standard - Ausgewogen"/>
                <ComboBoxItem Content="gpt-4o-mini" Tag="Schnell &amp; Günstig"/>
                <ComboBoxItem Content="gpt-5-nano" Tag="Sehr schnell"/>
                <ComboBoxItem Content="o3-mini" Tag="Reasoning-Modell"/>
                <ComboBoxItem Content="gemini-2.5-flash" Tag="Google - Schnell"/>
                <ComboBoxItem Content="gemini-2.5-pro" Tag="Google - Leistungsstark"/>
            </ComboBox>
        </StackPanel>

        <!-- Max Tokens -->
        <StackPanel Grid.Row="4">
            <Label Content="Max. Tokens:" FontWeight="Bold"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Slider x:Name="MaxTokensSlider" 
                        Grid.Column="0"
                        Minimum="100" 
                        Maximum="4000" 
                        Value="2000" 
                        TickFrequency="100" 
                        IsSnapToTickEnabled="True"
                        Margin="0,5,10,10"
                        VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" 
                           Text="{Binding ElementName=MaxTokensSlider, Path=Value, StringFormat='{}{0:F0}'}" 
                           Foreground="White" 
                           VerticalAlignment="Center"
                           Width="50"
                           TextAlignment="Right"/>
            </Grid>
        </StackPanel>

        <!-- Temperature -->
        <StackPanel Grid.Row="5">
            <Label Content="Temperatur (Kreativität):" FontWeight="Bold"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Slider x:Name="TemperatureSlider" 
                        Grid.Column="0"
                        Minimum="0" 
                        Maximum="2" 
                        Value="0.7" 
                        TickFrequency="0.1" 
                        IsSnapToTickEnabled="True"
                        Margin="0,5,10,10"
                        VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" 
                           Text="{Binding ElementName=TemperatureSlider, Path=Value, StringFormat='{}{0:F1}'}" 
                           Foreground="White" 
                           VerticalAlignment="Center"
                           Width="50"
                           TextAlignment="Right"/>
            </Grid>
            <TextBlock Text="0 = Deterministisch, 2 = Sehr kreativ" 
                       FontSize="10" 
                       Foreground="#888888" 
                       Margin="0,-5,0,0"/>
        </StackPanel>

        <!-- Test Connection -->
        <StackPanel Grid.Row="6" Margin="0,10,0,0">
            <Button x:Name="TestConnectionButton" 
                    Content="🔌 Verbindung testen" 
                    Click="TestConnectionButton_Click"
                    Style="{StaticResource SecondaryButton}"/>
            <TextBlock x:Name="TestResultTextBlock" 
                       Foreground="#4EC9B0" 
                       Margin="0,5,0,0" 
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="8" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="Speichern" 
                    x:Name="SaveButton" 
                    Width="100" 
                    Click="SaveButton_Click"/>
            <Button Content="Abbrechen" 
                    x:Name="CancelButton" 
                    Width="100" 
                    Click="CancelButton_Click"
                    Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
