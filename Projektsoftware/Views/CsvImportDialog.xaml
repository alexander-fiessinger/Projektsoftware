<Window x:Class="Projektsoftware.Views.CsvImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kontakt-Import (CSV)" Height="560" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="CSV-Kontakt-Import" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Margin="0,0,0,8" TextWrapping="Wrap" Foreground="#555"
                   Text="Erwartete Spalten (Header in erster Zeile): CompanyName, FirstName, LastName, Email, Phone, Street, ZipCode, City, Country. Trennzeichen: Semikolon oder Komma."/>

        <DockPanel Grid.Row="2" Margin="0,0,0,8">
            <Button DockPanel.Dock="Right" Content="Datei wählen..." Width="140" Click="Browse_Click"/>
            <TextBox x:Name="FilePathBox" IsReadOnly="True" Padding="4"/>
        </DockPanel>

        <DataGrid Grid.Row="3" x:Name="PreviewGrid" AutoGenerateColumns="True" IsReadOnly="True" HeadersVisibility="Column"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Button Content="Importieren" Width="120" Click="Import_Click"/>
            <Button Content="Schließen" Width="100" Margin="8,0,0,0" Click="Close_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
