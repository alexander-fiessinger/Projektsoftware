<Window x:Class="Projektsoftware.Views.OfferHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Angebots-Historie pro Kontakt" Height="560" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Angebots-Historie" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Kunde:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Button DockPanel.Dock="Right" Content="Anzeigen" Width="100" Margin="8,0,0,0" Click="Show_Click"/>
            <ComboBox x:Name="ContactCombo" DisplayMemberPath="DisplayName"/>
        </DockPanel>

        <DataGrid Grid.Row="2" x:Name="OffersGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nr." Binding="{Binding OfferNumber}" Width="110"/>
                <DataGridTextColumn Header="Titel" Binding="{Binding OfferTitle}" Width="*"/>
                <DataGridTextColumn Header="Datum" Binding="{Binding OfferDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                <DataGridTextColumn Header="Gültig bis" Binding="{Binding ValidUntil, StringFormat={}{0:dd.MM.yyyy}}" Width="110"/>
                <DataGridTextColumn Header="Betrag" Binding="{Binding TotalAmount, StringFormat={}{0:N2} €}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusDisplayText}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Schließen" Width="100" Click="Close_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
