<Window x:Class="Projektsoftware.Views.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🔐 Anmeldung" 
        Height="550" 
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="None"
        AllowsTransparency="True">
    <Border BorderBrush="{StaticResource PrimaryBrush}" 
            BorderThickness="2" 
            CornerRadius="10"
            Background="White">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo und Titel -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,20,0,30">
                <TextBlock Text="🔐" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Projektierungssoftware" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="Professional 2.0" 
                           FontSize="14" 
                           HorizontalAlignment="Center"
                           Foreground="#888"
                           Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Willkommenstext -->
            <TextBlock Grid.Row="1" 
                       Text="Bitte melden Sie sich an:"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Foreground="#555"
                       Margin="0,0,0,20"/>

            <!-- Benutzername -->
            <StackPanel Grid.Row="2" Margin="0,0,0,15">
                <TextBlock Text="Benutzername" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="5">
                    <TextBox x:Name="UsernameTextBox" 
                             Height="40"
                             FontSize="14"
                             Padding="10,8"
                             BorderThickness="0"
                             KeyDown="TextBox_KeyDown"/>
                </Border>
            </StackPanel>

            <!-- Passwort -->
            <StackPanel Grid.Row="3" Margin="0,0,0,15">
                <TextBlock Text="Passwort" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="5">
                    <PasswordBox x:Name="PasswordBox" 
                                 Height="40"
                                 FontSize="14"
                                 Padding="10,8"
                                 BorderThickness="0"
                                 PasswordChar="●"
                                 KeyDown="PasswordBox_KeyDown"/>
                </Border>
            </StackPanel>

            <!-- Fehlermeldung -->
            <Border Grid.Row="4" 
                    x:Name="ErrorPanel"
                    Background="#FFEBEE"
                    BorderBrush="#F44336"
                    BorderThickness="1"
                    CornerRadius="5"
                    Padding="10"
                    Margin="0,0,0,10"
                    Visibility="Collapsed">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="❌" FontSize="16" Margin="0,0,10,0"/>
                    <TextBlock x:Name="ErrorTextBlock" 
                               Text="Ungültige Anmeldedaten!"
                               Foreground="#D32F2F"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Buttons -->
            <Grid Grid.Row="6" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Abbrechen" 
                        Height="45"
                        Click="Cancel_Click"
                        Background="#9E9E9E"
                        Foreground="White"
                        FontSize="14"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="5"
                                                BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#757575"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="2"
                        Content="✓ Anmelden" 
                        Height="45"
                        Click="Login_Click"
                        Background="{StaticResource PrimaryBrush}"
                        Foreground="White"
                        FontSize="14"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="5"
                                                BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1976D2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>

