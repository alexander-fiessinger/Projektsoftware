<Window x:Class="Projektsoftware.Views.ProjectSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Projekt auswählen" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" x:Name="TitleTextBlock" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <!-- Description -->
        <TextBlock Grid.Row="1" x:Name="DescriptionTextBlock" 
                   TextWrapping="Wrap" Foreground="#666" Margin="0,0,0,15"/>

        <!-- Projects List -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <ListBox x:Name="ProjectsListBox" 
                     SelectionMode="Single"
                     MouseDoubleClick="ProjectsListBox_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="Kunde: " FontSize="11" Foreground="#666"/>
                                <TextBlock Text="{Binding ClientName}" FontSize="11" Foreground="#666" Margin="3,0,10,0"/>
                                <TextBlock Text="Status: " FontSize="11" Foreground="#666"/>
                                <TextBlock Text="{Binding Status}" FontSize="11" Foreground="#666" Margin="3,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Abbrechen" Width="100" Height="35" 
                    Margin="0,0,10,0" Click="Cancel_Click"
                    Style="{StaticResource ModernButton}"/>
            <Button Content="Auswählen" Width="100" Height="35" 
                    Click="Select_Click"
                    Background="#4CAF50" Foreground="White"
                    Style="{StaticResource ModernButton}"/>
        </StackPanel>
    </Grid>
</Window>
