<Window x:Class="Projektsoftware.Views.BudgetTrackingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Budget-Tracking" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Budget-Tracking pro Projekt" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Projekt:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Button DockPanel.Dock="Right" Content="Anzeigen" Width="100" Margin="8,0,0,0" Click="Show_Click"/>
            <ComboBox x:Name="ProjectCombo" DisplayMemberPath="Name"/>
        </DockPanel>

        <UniformGrid Grid.Row="2" Columns="4" Margin="0,0,0,10">
            <Border BorderBrush="#DDD" BorderThickness="1" Margin="4" Padding="10">
                <StackPanel>
                    <TextBlock Text="Geplante Stunden" FontSize="11" Foreground="#666"/>
                    <TextBlock x:Name="PlannedHoursText" Text="–" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border BorderBrush="#DDD" BorderThickness="1" Margin="4" Padding="10">
                <StackPanel>
                    <TextBlock Text="Tatsächliche Stunden" FontSize="11" Foreground="#666"/>
                    <TextBlock x:Name="ActualHoursText" Text="–" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border BorderBrush="#DDD" BorderThickness="1" Margin="4" Padding="10">
                <StackPanel>
                    <TextBlock Text="Geplantes Budget" FontSize="11" Foreground="#666"/>
                    <TextBlock x:Name="PlannedBudgetText" Text="–" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border BorderBrush="#DDD" BorderThickness="1" Margin="4" Padding="10">
                <StackPanel>
                    <TextBlock Text="Auslastung" FontSize="11" Foreground="#666"/>
                    <TextBlock x:Name="UtilizationText" Text="–" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <GroupBox Grid.Row="3" Header="Budget-Einträge">
            <DataGrid x:Name="EntriesGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Datum" Binding="{Binding EntryDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                    <DataGridTextColumn Header="Kategorie" Binding="{Binding Category}" Width="120"/>
                    <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Geplant" Binding="{Binding PlannedAmount, StringFormat={}{0:N2} €}" Width="120"/>
                    <DataGridTextColumn Header="Tatsächlich" Binding="{Binding ActualAmount, StringFormat={}{0:N2} €}" Width="120"/>
                    <DataGridTextColumn Header="Abweichung" Binding="{Binding VarianceFormatted}" Width="160"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Schließen" Width="100" Click="Close_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
